- name: check the variable live_sessions
  hosts: local
  connection: local
  
  vars:
    live_sessions: 4
  
  tasks:
  - name: more than 5 sessions
    ansible.builtin.debug:
      msg: "More than 5 sessions"
    when: live_sessions | int > 5

      

  
 
