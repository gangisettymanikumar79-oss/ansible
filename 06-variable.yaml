- name: demo
  hosts: localhost
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: Print course details
    ansible.builtin.debug:
      msg: "{{ course}} by {{trainer}}"
      

    

  
      
    


