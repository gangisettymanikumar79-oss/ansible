- name: demo play criecket
  hosts: localhost
  connection: local
  vars:
  - player.yaml
  
  tasks:
  - name: criecket
    ansible.builtin.debug:
      msg:"{{country}} by {{captain}} is {{player}}"


    

