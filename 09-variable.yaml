- name: demo on inventory
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: print name
      debug:
        msg: "Hi, I am learning  is {{ trainer }}, Duration is {{ duration }}"
        
  


