- name: install on package
  hosts: local
  connection: local
  tasks:
  - name: install package
    ansible.builtin.debug:
      msg: "{{item}}"
    loop:
    - git
    - docker
    - ansible
    - nginx
