---
- name: Test connectivity
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Verify Ansible connectivity
      ansible.builtin.ping:

    - name: Intentionally trigger an exception
      ansible.builtin.ping:
        data: crash

