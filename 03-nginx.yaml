- name:  install and start nginx
  hosts: localhost
  become: yes
  tasks:
   - name:  install nginx
     ansible.builtin.package:
      name: nginx
      state: present
   - name: start nginx service
     ansible.builtin.package:
      name: nginx
      state: started
      enabled: yes
      





