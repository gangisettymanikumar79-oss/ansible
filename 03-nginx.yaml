- name: dnf install the nginx
  hosts: local
  tasks:
  - name:  install nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: start nginx
    server:
      name: nginx
      state: present
      enabled: yes
      





