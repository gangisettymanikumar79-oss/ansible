- name: demo on shell vs command
  hosts: local
  connection: local
  tasks:
  - name: shell  test
    shell: cat /etc/passwd | greep root
    register: user_output
  
  - name: print output
    ansible.builtin.debug:
      var: user_output.stdout
   
    
