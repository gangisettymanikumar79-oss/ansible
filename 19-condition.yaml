- name: demo on filters
  hosts: local
  connection: local
  vars:
  topices: "Linux, ansible,docker, git"
  tasks: 
  - name: convert to filters
    ansible.builtin.debug:
      msg: "sting Topices: {{topices | split(',')}}"
